---
import { getLangFromUrl, useTranslations } from '../../i18n/utils';
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Refactored to pass unique traceability details per product
const products = [
  {
    id: 'fitness',
    name: t('products.fitness.name'),
    desc: t('products.fitness.desc'),
    image: '/assets/images/fitness-pack.jpg',
    traceName: t('products.trace.name'),
    traceAge: t('products.trace.age_val'),
    traceHobby: t('products.trace.hobby_val')
  },
  {
    id: 'bbq',
    name: t('products.bbq.name'),
    desc: t('products.bbq.desc'),
    image: '/assets/images/bbq-pack.jpg',
    traceName: t('products.bbq.trace.name'),
    traceAge: t('products.bbq.trace.age_val'),
    traceHobby: t('products.bbq.trace.hobby_val')
  }
];
---

<div class="page-banner">
  <img src="/assets/images/sliced.jpg" alt="Healthy macro-friendly fitness meals" fetchpriority="high" decoding="sync" />
</div>

<section id="premium-products">
  <div class="container">
    <h1>{t('products.title')}</h1>
    <div class="breed-grid">
      {products.map((product) => (
        <div class="breed-card" id={`product-${product.id}`}>
          <img src={product.image} alt={product.name} class="breed-img" />
          <div class="breed-info">
            <h3>{product.name}</h3>
            <p>{product.desc}</p>

            <div class="traceability-box">
              <div class="trace-title">{t('products.trace.title')}</div>
              <ul class="trace-details">
                <li>
                  <span class="trace-label">{t('products.trace.batch')}</span>
                  <span>{product.traceName}</span>
                </li>
                <li>
                  <span class="trace-label">{t('products.trace.age')}</span>
                  <span>{product.traceAge}</span>
                </li>
                <li>
                  <span class="trace-label">{t('products.trace.hobby')}</span>
                  <span>{product.traceHobby}</span>
                </li>
              </ul>
            </div>

            <div class="feature-badge badge-freshness" set:html={t('products.freshness')} />

            <button class="btn add-to-cart" data-lang={lang} data-track="shop--add-to-cart">{t('breeds.add_to_cart')}</button>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  const buttons = document.querySelectorAll('.add-to-cart');
  buttons.forEach(button => {
    button.addEventListener('click', () => {
      const lang = button.getAttribute('data-lang');
      const faqPath = lang === 'zh-hk' ? '/faq' : '/en/faq';
      window.location.href = faqPath;
    });
  });
</script>